# 贡献 Director Skills

感谢你愿意让 AI 视频创作流程变得更可执行、可复用、可验证。

## 新增 Skill

1. 从 `templates/SKILL.template.md` 开始，在仓库根目录新建 kebab-case 命名的 Skill 文件夹。
2. 将模板复制为 `<skill-name>/SKILL.md`，替换所有占位内容。
3. 只添加该 Skill 真正需要的 `scripts/`、`references/` 或 `templates/`。
4. 用至少一个真实任务跑通工作流，记录输入、关键决策、结果和失败边界。
5. 更新根 `README.md` 目录。未验证的 Skill 必须标记为“计划中”或“实验中”。

## 内容要求

- 用可执行的指令代替抽象口号。
- 区分原始资料、推断、重建提示词和真实生成结果。
- 不提交 API Key、Cookie、未脱敏账号信息或未获授权的商业素材。
- 第三方代码、模板或方法必须标明来源并遵守许可证。
- 存在模型或平台限制时，在 `SKILL.md` 中明确说明。

## 提交前检查

- [ ] `SKILL.md` 的 `name` 与目录名一致
- [ ] `description` 写清了触发场景
- [ ] 所有相对路径都可用
- [ ] 脚本提供 `--help` 或等价使用说明
- [ ] 真实案例已验证，失败情况已记录
- [ ] 未包含密钥、个人数据和无授权素材
